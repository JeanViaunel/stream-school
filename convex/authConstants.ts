/** Must match `issuer` in `auth.config.ts` and JWT claims in `convexJwt.ts`. */
export const CONVEX_AUTH_ISSUER = "https://streamschool.app";
/** Must match `applicationID` in `auth.config.ts` and JWT `aud` claim. */
export const CONVEX_AUTH_AUDIENCE = "stream-school";
